% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/person_profile.R
\name{build_person_profile}
\alias{build_person_profile}
\title{Reduce an EMP_ID cluster to a canonical person profile}
\usage{
build_person_profile(
  df,
  cols = synthid_cols(),
  emp_id = "EMP_ID",
  title = "title.standard",
  state = NULL,
  ntee = NULL,
  overrides = NULL
)
}
\arguments{
\item{df}{A linked panel: the output of \code{\link[=link_panel]{link_panel()}} (or any frame carrying
\code{EMP_ID} plus parsed name components). Multiple rows per \code{EMP_ID}.}

\item{cols}{Column mapping; see \code{\link[=synthid_cols]{synthid_cols()}}. Uses \code{org_id}, \code{year},
\code{name}, and the name-component features (\code{first_name}, \code{middle_name},
\code{last_name}, \code{suffix}, \code{gender}).}

\item{emp_id}{Name of the person-cluster id column (default \code{"EMP_ID"}).}

\item{title}{Name of the standardized-title column, classified into a coarse
role via \code{\link[=classify_title_role]{classify_title_role()}} (default \code{"title.standard"}; skipped if
absent).}

\item{state, ntee}{Optional column names for organization geography and NTEE
industry. When present they are carried into the profile as variant sets and
are available as relaxable blocking dimensions; when \code{NULL} or absent they
are skipped (join them from the BMF beforehand if you want them).}

\item{overrides}{Passed to \code{\link[=classify_title_role]{classify_title_role()}} for title tail cases.}
}
\value{
A data frame, one row per \code{EMP_ID}, with scalar summary columns and
list-columns for the variant sets:
\describe{
\item{\code{EMP_ID}, \code{n_records}, \code{n_orgs}, \code{n_years}, \code{first_year},
\code{last_year}}{Cluster size and span.}
\item{\code{last_name}, \code{last_name_variants}, \code{last_name_keys}}{Modal surname;
the distinct surnames observed; their phonetic (Soundex) blocking keys.}
\item{\code{first_name}, \code{first_name_variants}, \code{first_name_roots},
\code{first_initial}}{Most-complete formal given name; distinct given names;
their canonical nickname roots (\code{\link[=first_name_roots]{first_name_roots()}}); leading initial.}
\item{\code{middle_initials}, \code{suffixes}, \code{gender}}{Union of middle initials and
suffixes; modal gender (\code{NA} on tie/unknown).}
\item{\code{name_variants}}{Distinct full-name strings seen for the person.}
\item{\code{primary_role}, \code{roles}, \code{is_board}}{Modal coarse role; the role set;
whether the person \emph{ever} held a \code{BOARD} seat.}
\item{\code{titles}, \code{orgs}, \code{years}}{Distinct standardized titles,
organization ids, and tax years for the person.}
\item{\code{states}, \code{ntee}}{Present only when \code{state}/\code{ntee} are supplied.}
}
List-columns hold one character vector per person; unnest \code{last_name_keys}
(with \code{EMP_ID}) to get the long key table for the stage-1 hash join.
}
\description{
Collapses every record of a within-organization person (all rows sharing an
\code{EMP_ID}, as stamped by \code{\link[=link_panel]{link_panel()}}) into one structured profile row for
cross-organization linkage. Deliberately does \strong{not} flatten the cluster to a
single canonical name string: the variant \emph{sets} are what carry a person from
one organization's filing to another's (a maiden/married surname, an OCR
variant, a nickname), so each name field keeps both a deterministic canonical
value (for display and as a primary blocking key) and the full set of observed
variants (for blocking recall and pairwise scoring).
}
\details{
The profile is the unit compared in the cross-org stage. Stage 1 (candidate
generation) is a hash/inverted-index lookup on blocking keys rather than a
materialized within-block cross product: a profile emits one key per surname
variant (\code{last_name_keys}, phonetic), lands in every matching bucket, and only
profiles sharing a bucket are scored -- so maiden/married/OCR variants are not
silently dropped, and a common surname never materializes an all-pairs block.
}
\examples{
\dontrun{
linked <- link_panel(panel)
profiles <- build_person_profile(linked)
# long blocking-key table for the cross-org hash join:
keys <- do.call(rbind, Map(function(id, k) data.frame(EMP_ID = id, key = k),
                           profiles$EMP_ID, profiles$last_name_keys))
}
}
\seealso{
\code{\link[=classify_title_role]{classify_title_role()}}, \code{\link[=first_name_roots]{first_name_roots()}}
}
