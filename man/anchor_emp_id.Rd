% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anchor.R
\name{anchor_emp_id}
\alias{anchor_emp_id}
\title{Mint an anchored EMP_ID from an anchor key}
\usage{
anchor_emp_id(anchor_key, keyspec = "v1")
}
\arguments{
\item{anchor_key}{Character vector of anchor keys.}

\item{keyspec}{Version tag baked into the hashed string; bump it if the id
recipe ever changes.}
}
\value{
A character vector of \code{EMP-...} ids, one per \code{anchor_key}.
}
\description{
Hashes one or more anchor keys (\code{\link[=emp_anchor_key]{emp_anchor_key()}}) into \code{EMP-...} person ids.
A scheme tag is folded into the hashed string so anchored ids occupy a
different keyspace than the legacy hash-of-membership ids and the two can never
silently collide.
}
\examples{
anchor_emp_id(c("PYID-abc", "PYID-def"))
}
\seealso{
\code{\link[=emp_anchor_key]{emp_anchor_key()}}, \code{\link[=create_emp_ids]{create_emp_ids()}}
}
