% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/incremental.R
\name{match_to_profiles}
\alias{match_to_profiles}
\title{Match a new wave of people to an existing frozen person set}
\usage{
match_to_profiles(wave, existing, weights = default_weights(),
  threshold = 7, surname_weight = NULL, max_block_size = 5000L,
  verbose = FALSE)
}
\arguments{
\item{wave}{Profiles of the internally linked new wave (\code{\link[=build_person_profile]{build_person_profile()}}
over \code{link_panel(new)}).}

\item{existing}{Profiles of the frozen panel; should carry \code{EMP_ANCHOR} (via
\code{\link[=build_person_profile]{build_person_profile()}} on an anchored \code{\link[=link_panel]{link_panel()}} output) so matches can
inherit it.}

\item{weights}{Base match weights; see \code{\link[=default_weights]{default_weights()}}.}

\item{threshold}{Minimum score to accept a wave<->existing match (default \code{7},
the within-org default; \strong{profile-vs-profile scores want their own tuning}).}

\item{surname_weight}{Optional precomputed surname-rarity vector; the population
weight over \code{rbind(existing, wave)} if \code{NULL}.}

\item{max_block_size}{Oversize-bucket guard for \code{\link[=same_org_candidate_pairs]{same_org_candidate_pairs()}}.}

\item{verbose}{Print stage sizes.}
}
\value{
A list:
\describe{
\item{\code{matched}}{\code{wave_emp_id}, \code{existing_emp_id}, \code{existing_emp_anchor}
(if available), \code{score} -- one row per accepted match.}
\item{\code{unmatched}}{Character vector of wave \code{EMP_ID}s with no accepted
match (the first-time people, to be minted fresh downstream).}
\item{\code{review}}{Above-threshold pairs that were \emph{not} accepted, with a
\code{reason}: \code{"ambiguous"} (lost the greedy one-to-one -- a second plausible
match worth a look) or \code{"invariant_collision"} (strong score but the two
share an org-year, so they cannot be one person).}
\item{\code{report}}{Counts for the run.}
}
}
\description{
The asymmetric core of incremental linkage (see \verb{dev/PLAN-incremental-waves.md}).
Given profiles for an already-linked panel (\code{existing}, carrying durable
\code{EMP_ID}/\code{EMP_ANCHOR}) and profiles for a newly linked wave (\code{wave}, with its
own provisional wave-local \code{EMP_ID}s from a fresh \code{\link[=link_panel]{link_panel()}} over the wave
alone), decide which wave-people are returning existing people and which are
new. It does \strong{not} re-link existing-vs-existing and never renames an existing
id; it only maps wave ids onto existing ones (or leaves them to be minted
fresh).
}
\details{
Pipeline: same-org surname blocking (\code{\link[=same_org_candidate_pairs]{same_org_candidate_pairs()}}) -> score the
candidate pairs from their variant sets (\code{\link[=score_candidate_pairs]{score_candidate_pairs()}}) -> drop
pairs that violate the one-record-per-(org,year) invariant (a wave-person and an
existing person who both file in the same org \emph{and} year cannot be the same
person) -> accept the rest greedily one-to-one (\code{\link[=greedy_one_to_one]{greedy_one_to_one()}}).

Surname evidence is weighted by \emph{population} rarity (\code{\link[=population_surname_weight]{population_surname_weight()}})
over the combined profile set, not within-org frequency: at the match stage both
people are already distinct identities, so the question is how surprising the
surname agreement is across the wave boundary (\code{SMITH} weak, \code{GANTSOUDES}
strong) -- the same logic as cross-org linkage. Within-org family-board
separation was already handled when the wave was linked internally.
}
\seealso{
\code{\link[=same_org_candidate_pairs]{same_org_candidate_pairs()}}, \code{\link[=score_candidate_pairs]{score_candidate_pairs()}},
\code{\link[=population_surname_weight]{population_surname_weight()}}
}
