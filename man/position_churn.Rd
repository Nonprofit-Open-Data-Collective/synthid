% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transitions.R
\name{position_churn}
\alias{position_churn}
\title{Measure churn within a position over time}
\usage{
position_churn(
  df,
  position = NULL,
  role = NULL,
  cols = synthid_cols(),
  id = "EMP_ID",
  title = "title.standard",
  by_org = TRUE,
  match = c("exact", "regex", "fuzzy"),
  min_sim = 0.9,
  overrides = NULL
)
}
\arguments{
\item{df}{A linked panel: output of \code{\link[=link_panel]{link_panel()}}, carrying the id column.}

\item{position}{Character vector of standardized titles (or \code{NULL}).}

\item{role}{Character vector of coarse roles -- \code{"BOARD"}, \code{"OFFICER"},
\code{"STAFF"}, \code{"OTHER"} (or \code{NULL}). For a board pass \code{role = "BOARD"}. At least
one of \code{position}/\code{role} is required.}

\item{cols}{Column mapping; see \code{\link[=synthid_cols]{synthid_cols()}} (uses \code{org_id}, \code{year}).}

\item{id}{Person id column to count (default \code{"EMP_ID"}). Pass a cross-org id to
pool churn across organizations with \code{by_org = FALSE}.}

\item{title}{Standardized-title column (default \code{"title.standard"}).}

\item{by_org}{If \code{TRUE} (default) churn is computed per organization; if \code{FALSE}
it is pooled over the whole panel (population churn for the position).}

\item{match}{How \code{position} is matched: \code{"exact"} (default), \code{"regex"}, or
\code{"fuzzy"} (Jaro-Winkler at \code{min_sim}).}

\item{min_sim}{Similarity floor for \code{match = "fuzzy"}.}

\item{overrides}{Passed to \code{\link[=classify_title_role]{classify_title_role()}} when selecting by \code{role}.}
}
\value{
A data frame of class \code{synthid_churn}, one row per group-year (a group is
an organization when \code{by_org}, else the whole panel):
\describe{
\item{org id (when \code{by_org}), year (named per \code{cols})}{The group and year.}
\item{\code{n_current}}{Distinct ids holding the position that year.}
\item{\code{n_new}}{Holders not present the prior filing year (\code{NA} in the first).}
\item{\code{n_left}}{Prior-year holders absent this year (\code{NA} in the first).}
\item{\code{n_stable}}{Holders present both years (\code{NA} in the first).}
\item{\code{n_prev}}{Holders the prior filing year.}
\item{\code{retention}, \code{turnover}}{\code{n_stable / n_prev} and \code{n_new / n_current}.}
}
A run summary is attached as attribute \code{"synthid_churn"}.
}
\description{
Turnover accounting for a position -- most often a board -- across an
organization's filing years. For each year it reports how many distinct people
held the position, and, relative to the prior filing year, how many are new, how
many have left, and how many are stable (held it both years). This is the
body-level complement to \code{\link[=flag_position_transitions]{flag_position_transitions()}}: churn counts a whole
set's turnover, transitions name the individual handovers within a one- or
two-seat post.
}
\details{
The prior filing year is the organization's own previous year in \code{df}, so a year
in which the position falls to zero holders (a board that skipped reporting, say)
still produces a row -- with every prior holder counted as \code{n_left}. Rows begin
at each group's first year with a holder; earlier empty years are dropped.
}
\examples{
\dontrun{
linked <- link_panel(panel)
position_churn(linked, role = "BOARD")               # board churn per org
position_churn(linked, position = "CEO", by_org = FALSE)  # pooled CEO churn
}
}
\seealso{
\code{\link[=flag_position_transitions]{flag_position_transitions()}}, \code{\link[=flag_promotions]{flag_promotions()}}.
}
