% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anchor.R
\name{emp_anchor_key}
\alias{emp_anchor_key}
\title{Choose a cluster's stable anchor key}
\usage{
emp_anchor_key(year, object_id = NULL, table_id = NULL, row_uid,
  keyspec = "v1")
}
\arguments{
\item{year}{Tax years of the cluster's records (coerced to integer).}

\item{object_id, table_id}{Native filing keys (\code{OBJECTID}, \code{TABLE_ID}) of the
records, or \code{NULL} when unavailable; the anchor then falls back to \code{row_uid}.}

\item{row_uid}{Per-record fallback keys (e.g. \code{\link[=build_id_string]{build_id_string()}} output),
used for tie-breaking and as the anchor key when native keys are absent.}

\item{keyspec}{Version tag forwarded to \code{\link[=person_year_id]{person_year_id()}}.}
}
\value{
A single character anchor key.
}
\description{
Reduces the member records of one person cluster to a single deterministic
\emph{anchor key} -- the durable string that mints the person's \code{EMP_ID} under the
anchored id scheme (\code{\link[=anchor_emp_id]{anchor_emp_id()}}). Unlike the legacy hash-of-membership
recipe, an anchor depends only on the person's \emph{first-seen} record, so adding
later records (a new wave) -- or even earlier records (a backfill) -- to a
person never changes their id.
}
\details{
The anchor record is the cluster's deterministic earliest: earliest tax \code{year},
ties broken by preferring a record that carries a native filing key, then by
\code{object_id}, \code{table_id}, and finally the fallback \code{row_uid}. The returned key
is that record's \code{\link[=person_year_id]{person_year_id()}} when both native keys are present, else its
\code{row_uid}. Missing years sort last, so a record with a known year always
out-anchors one without.

\code{emp_anchor_key()} operates on the members of a \emph{single} cluster (equal-length
vectors) and returns one key; callers split by cluster and apply it per group.
}
\examples{
emp_anchor_key(year = c(2021, 2019, 2020),
               object_id = c("O3", "O1", "O2"),
               table_id  = c("T3", "T1", "T2"),
               row_uid   = c("r3", "r1", "r2"))   # anchors on the 2019 record
}
\seealso{
\code{\link[=anchor_emp_id]{anchor_emp_id()}}, \code{\link[=person_year_id]{person_year_id()}}
}
