% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link_cross_org.R
\name{score_candidate_pairs}
\alias{score_candidate_pairs}
\title{Score cross-organization candidate pairs from their profiles}
\usage{
score_candidate_pairs(
  cand,
  profiles,
  weights = default_weights(),
  surname_weight = NULL,
  components = FALSE
)
}
\arguments{
\item{cand}{Candidate pairs from \code{\link[=candidate_pairs]{candidate_pairs()}} (\code{emp_a}, \code{emp_b}, ...).}

\item{profiles}{The profiles those ids index (from \code{\link[=build_person_profile]{build_person_profile()}}).}

\item{weights}{Named base weights; see \code{\link[=default_weights]{default_weights()}}.}

\item{surname_weight}{Optional precomputed \code{\link[=population_surname_weight]{population_surname_weight()}} vector;
built from \code{profiles} if \code{NULL}.}

\item{components}{If \code{TRUE}, also return the per-field similarity columns
(\code{sim_last}, \code{sim_first}, ...) for inspection/tuning.}
}
\value{
\code{cand} with an added numeric \code{score} column (and, if \code{components}, the
per-field similarities), sorted by descending score.
}
\description{
Stage 2 of cross-org linkage: turns each candidate pair from \code{\link[=candidate_pairs]{candidate_pairs()}}
into a Fellegi-Sunter-style additive match score, reusing the within-org
weighting (\code{\link[=default_weights]{default_weights()}}, \code{\link[=score_pairs]{score_pairs()}}) but computing every field
similarity from the person \emph{variant sets} rather than a single string:
\itemize{
\item \code{last_name} -- best match over \verb{last_name_variants_a x _b}
(\code{\link[=compare_two_names]{compare_two_names()}}), frequency-adjusted by the \emph{population} surname
rarity (\code{\link[=population_surname_weight]{population_surname_weight()}});
\item \code{first_name} -- best match over \code{first_name_variants} with nickname and
initial handling (\code{\link[=compare_first_names]{compare_first_names()}});
\item \code{middle_name}, \code{suffix} -- set overlap of the initials / suffixes
(agree / disagree / neutral-if-missing);
\item \code{gender} -- exact agreement of the modal genders.
}
Title is deliberately excluded -- per the interlock design it is a downstream
edge filter, not a linkage signal.
}
\seealso{
\code{\link[=candidate_pairs]{candidate_pairs()}}, \code{\link[=link_cross_org]{link_cross_org()}}
}
