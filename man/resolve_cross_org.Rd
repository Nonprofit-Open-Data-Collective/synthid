% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link_cross_org.R
\name{resolve_cross_org}
\alias{resolve_cross_org}
\title{Resolve scored cross-org edges into cross-organization person clusters}
\usage{
resolve_cross_org(edges, profiles)
}
\arguments{
\item{edges}{Accepted edges (\code{emp_a}, \code{emp_b}, \code{score}).}

\item{profiles}{Profiles supplying each person's organization.}
}
\value{
A data frame \code{EMP_ID}, \code{XORG_ID} (deterministic cross-org person id;
singletons get their own), \code{XORG_N_ORGS}, \code{XORG_N_TITLES} (distinct titles),
\code{XORG_N_YEARS} (distinct calendar years), and \code{XORG_N_RECORDS} (total filing
rows) across the whole cross-org person. \code{rejected_edges} count is attached
as an attribute.
}
\description{
Closes the accepted cross-org edges into connected components, enforcing the
invariant that no cross-org person may contain two \code{EMP_ID}s from the \emph{same}
organization -- two distinct within-org identities in one organization are two
different people by construction, so a transitive chain that would merge them
is rejected (highest-score edges kept first), exactly mirroring the year guard
in the within-org resolver.
}
\keyword{internal}
