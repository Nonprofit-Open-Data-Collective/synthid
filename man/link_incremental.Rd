% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/incremental.R
\name{link_incremental}
\alias{link_incremental}
\title{Integrate a new wave of data into an already-linked panel}
\usage{
link_incremental(existing, new, cols = synthid_cols(),
  weights = default_weights(), method = c("weighted", "em"),
  link_threshold = 7, match_threshold = 7, prob_threshold = 0.5,
  surname_weight = NULL, max_block_size = 5000L, verbose = FALSE)
}
\arguments{
\item{existing}{A panel already linked under the anchored scheme: must carry
\code{EMP_ID} \strong{and} \code{EMP_ANCHOR} (run \code{\link[=link_panel]{link_panel()}}, or migrate a legacy panel
with \code{\link[=remint_anchored]{remint_anchored()}}, first).}

\item{new}{The new wave: parsed records in the same schema as \code{existing}, with
no ids yet.}

\item{cols}{Column mapping; see \code{\link[=synthid_cols]{synthid_cols()}}.}

\item{weights}{Base match weights; see \code{\link[=default_weights]{default_weights()}}.}

\item{method}{Wave-internal linkage method, passed to \code{\link[=link_panel]{link_panel()}}.}

\item{link_threshold}{Threshold for the wave-internal \code{\link[=link_panel]{link_panel()}}
(\code{method = "weighted"}).}

\item{match_threshold}{Threshold for the wave<->existing match
(\code{\link[=match_to_profiles]{match_to_profiles()}}); wants its own tuning (profile-vs-profile scale).}

\item{prob_threshold}{Posterior threshold for the wave-internal link when
\code{method = "em"}.}

\item{surname_weight}{Optional precomputed surname-rarity vector for the match
stage; see \code{\link[=match_to_profiles]{match_to_profiles()}}.}

\item{max_block_size}{Oversize-bucket guard for the match blocker.}

\item{verbose}{Print progress.}
}
\value{
A list:
\describe{
\item{\code{new_stamped}}{The genuinely-new wave rows (re-loads removed) with
\code{EMP_ID} and \code{EMP_ANCHOR} stamped -- inherited for returning people,
freshly minted for first-time people. \code{rbind()} onto \code{existing} (after
aligning columns) to grow the panel; recompute any per-person counts on
the merged result.}
\item{\code{review}}{Ambiguous / invariant-collision pairs from the match, for
eyeballing (see \code{\link[=match_to_profiles]{match_to_profiles()}}); never auto-merged.}
\item{\code{unmatched}}{Wave-local \code{EMP_ID}s judged first-time people.}
\item{\code{wave_id_map}}{\code{wave} (wave-local id) -> \code{final_id}, \code{final_anchor},
\code{matched} -- the audit trail of every wave-person's resolution.}
\item{\code{report}}{Counts for the run.}
}
}
\description{
End-to-end incremental linkage: attach a new wave of records (a later year, a
multi-year batch, or a backfill of missing years) to a frozen, already-linked
panel, \strong{reusing the existing person ids instead of re-minting them}. This is
the third linkage mode -- distinct from the initial batch build (\code{\link[=link_panel]{link_panel()}},
all record-pairs within an org) and cross-org interlock (\code{\link[=link_cross_org]{link_cross_org()}},
profiles across orgs); see \verb{dev/PLAN-incremental-waves.md}.
}
\details{
The wave is first linked \strong{internally} (\code{\link[=link_panel]{link_panel()}} over \code{new} alone), so a
first-time person appearing across several wave years becomes one wave-person;
the wave-people are then matched to the existing people
(\code{\link[=match_to_profiles]{match_to_profiles()}}). A matched wave-person \strong{inherits} the existing
\code{EMP_ID} and \code{EMP_ANCHOR} -- which is what freezes an id across a backfill: an
earlier record for a known person takes that person's existing anchor rather
than re-anchoring. An unmatched wave-person keeps its freshly minted anchored
wave-local id (a genuinely new person).

\code{new} rows whose native \verb{(OBJECTID, TABLE_ID)} key already appears in \code{existing}
are exact re-loads, not new data; they are dropped with a warning so they do
not spawn duplicate people.
}
\examples{
\dontrun{
existing <- link_panel(panel_2019_2023)       # anchored ids + EMP_ANCHOR
wave     <- readr::read_csv("PANEL-2024.csv")
res <- link_incremental(existing, wave)
merged <- rbind(existing[names(res$new_stamped)], res$new_stamped)
subset(res$review, reason == "ambiguous")      # spot-check borderline matches
}
}
\seealso{
\code{\link[=match_to_profiles]{match_to_profiles()}}, \code{\link[=link_panel]{link_panel()}}, \code{\link[=remint_anchored]{remint_anchored()}}
}
