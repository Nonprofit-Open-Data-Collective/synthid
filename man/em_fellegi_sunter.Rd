% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{em_fellegi_sunter}
\alias{em_fellegi_sunter}
\title{Fellegi-Sunter latent-class EM (unsupervised)}
\usage{
em_fellegi_sunter(G, tol = 1e-06, max_iter = 200)
}
\arguments{
\item{G}{Logical matrix (rows = pairs, cols = fields), \code{NA} allowed.}

\item{tol, max_iter}{Convergence tolerance and iteration cap.}
}
\value{
List with \code{m}, \code{u} (named by column), \code{p}, and \code{iters}.
}
\description{
Fits the classic two-class (match / non-match) latent model to a matrix of
binary field agreements under the conditional-independence assumption,
learning by EM -- with no labels -- the prior match rate \code{p} and, per field,
the agreement probability among matches (\code{m}) and non-matches (\code{u}). This is
the model \code{reclin2::problink_em()} implements; it is inlined here to work
directly on a plain agreement matrix. Missing agreements (\code{NA}) contribute no
evidence for that field/row.
}
\keyword{internal}
