% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link.R
\name{score_year_pair}
\alias{score_year_pair}
\title{Score every candidate pair between two single-year frames}
\usage{
score_year_pair(dx, dy, feats, comparators, weights)
}
\arguments{
\item{dx, dy}{Prepared single-year frames (subsets of a \code{\link[=prepare_panel]{prepare_panel()}}
\code{work} frame).}

\item{feats}{Feature column names to compare.}

\item{comparators}{Comparator list from \code{\link[=match_comparators]{match_comparators()}}.}

\item{weights}{Named base weights.}
}
\value{
A data frame with \code{ix}, \code{iy} (row indices into \code{dx}, \code{dy}) and \code{score},
or \code{NULL} if there are no candidate pairs.
}
\description{
Blocks on organization, compares the configured fields, and returns the
surname-frequency-adjusted score for \emph{every} candidate pair (no threshold, no
selection). Shared by \code{\link[=link_year_pairs]{link_year_pairs()}} and \code{\link[=candidate_scores]{candidate_scores()}} so the two
can never drift apart.
}
\keyword{internal}
