% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refresh_cross_org.R
\name{refresh_cross_org}
\alias{refresh_cross_org}
\title{Refresh cross-organization interlocks after integrating a new wave}
\usage{
refresh_cross_org(merged, prior = NULL, cols = synthid_cols(),
  state = NULL, ntee = NULL, weights = default_weights(), threshold = 7,
  max_block_size = 5000L, verbose = FALSE)
}
\arguments{
\item{merged}{The grown, linked panel: \code{existing} with the wave's \code{new_stamped}
rows appended (each row carries \code{EMP_ID}). See \code{\link[=link_incremental]{link_incremental()}}.}

\item{prior}{Optional previous cross-org result to diff against: either the
\code{assignment} data frame or a whole \code{\link[=link_cross_org]{link_cross_org()}} result list. \code{NULL}
skips the diff.}

\item{cols}{Column mapping; see \code{\link[=synthid_cols]{synthid_cols()}}.}

\item{state, ntee}{Optional geography / industry columns for tighter blocking;
see \code{\link[=link_cross_org]{link_cross_org()}}.}

\item{threshold, max_block_size, weights, verbose}{Passed to \code{\link[=link_cross_org]{link_cross_org()}}.}
}
\value{
A list: \code{xorg} (the full \code{\link[=link_cross_org]{link_cross_org()}} result), \code{assignment} (its
\code{EMP_ID} -> \code{XORG_ID} map, for convenience), and \code{changes} (the
\code{\link[=diff_cross_org]{diff_cross_org()}} summary, or \code{NULL} when \code{prior} is not supplied).
}
\description{
A new wave (\code{\link[=link_incremental]{link_incremental()}}) can create fresh cross-org interlocks -- a
new person who sits on a second org's board, or a returning person who picks up
an outside seat. This re-runs the cross-org linkage (\code{\link[=link_cross_org]{link_cross_org()}}) over
the grown panel and, when given the previous assignment, reports exactly what
changed. Because \code{XORG_ID} is anchored to each interlock's founding member
(\code{\link[=anchor_xorg_id]{anchor_xorg_id()}}), an unchanged interlock keeps its id across refreshes;
only genuinely new or grown clusters move.
}
\details{
This is a full re-link of the merged panel (cheap: the cross-org stage is
hash-blocked, not all-pairs), not a fully-incremental cross-org matcher -- a
later optimization would score only the wave-affected profiles against the
rest. For now the diff makes the full re-run's \emph{effect} incremental to read.
}
\examples{
\dontrun{
xo0 <- link_cross_org(existing, state = "state", ntee = "ntee")   # before
inc <- link_incremental(existing, wave)
merged <- rbind(existing[names(inc$new_stamped)], inc$new_stamped)
ref <- refresh_cross_org(merged, prior = xo0, state = "state", ntee = "ntee")
ref$changes$newly_interlocking_persons     # who spans a new org after the wave
}
}
\seealso{
\code{\link[=link_incremental]{link_incremental()}}, \code{\link[=link_cross_org]{link_cross_org()}}
}
