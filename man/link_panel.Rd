% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link_panel.R
\name{link_panel}
\alias{link_panel}
\title{Assign stable cross-year person identifiers to a compensation panel}
\usage{
link_panel(
  df,
  cols = synthid_cols(),
  weights = default_weights(),
  threshold = 7,
  method = c("weighted", "em"),
  prob_threshold = 0.5,
  verbose = FALSE
)
}
\arguments{
\item{df}{A stacked multi-year panel: the parsed output of \pkg{titleclassifier}
and \pkg{peopleparser}. Must contain at least the organization id, tax year,
and full name columns named by \code{cols}.}

\item{cols}{Column mapping; see \code{\link[=synthid_cols]{synthid_cols()}}.}

\item{weights}{Named base match weights; see \code{\link[=default_weights]{default_weights()}}.}

\item{threshold}{Minimum match score to link two records when
\code{method = "weighted"} (default \code{7}, chosen by tuning against a labeled slice;
see \code{dev/tune_threshold.R}).}

\item{method}{Scoring method: \code{"weighted"} (default) uses the hand-set
Fellegi-Sunter-style additive score; \code{"em"} fits an unsupervised
Fellegi-Sunter latent-class model (\code{\link[=fit_match_model]{fit_match_model()}}) to the candidate
comparisons and links by the learned posterior match probability, also
attaching a per-person link confidence.}

\item{prob_threshold}{Minimum posterior match probability to link two records
when \code{method = "em"} (default \code{0.5}).}

\item{verbose}{Print progress.}
}
\value{
\code{df} with added columns: \code{EMP_ID} (stable person id, minted from the
person's anchor record so it survives new waves and backfills; see
\code{\link[=emp_anchor_key]{emp_anchor_key()}}), \code{EMP_ANCHOR} (the anchor key behind that id),
\code{EMP_N_RECORDS}, \code{EMP_N_YEARS}, and -- for \code{method = "em"} -- \code{EMP_LINK_CONF} (the weakest
posterior probability among the links holding the person's cluster together;
\code{NA} for single-record persons), a ready-made confidence input for a
downstream panel model. Diagnostics are attached as the \code{"synthid"} attribute
(see \code{\link[=synthid_report]{synthid_report()}}).
}
\description{
End-to-end pipeline: prepare the panel, link records across every pair of tax
years within each organization, resolve the accepted links into person
clusters (one record per organization-year, enforced), and stamp one
deterministic \code{EMP_ID} on every record.
}
\examples{
\dontrun{
panel <- readr::read_csv("PANEL-2019-2021.csv")
linked <- link_panel(panel)                 # hand-weighted
linked_em <- link_panel(panel, method = "em")
fs_weights(attr(linked_em, "synthid")$model) # learned weights
}
}
