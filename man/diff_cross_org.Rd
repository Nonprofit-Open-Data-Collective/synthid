% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refresh_cross_org.R
\name{diff_cross_org}
\alias{diff_cross_org}
\title{Diff two cross-org assignments}
\usage{
diff_cross_org(prior, new)
}
\arguments{
\item{prior, new}{Cross-org assignment data frames (the \code{assignment} element of
a \code{\link[=link_cross_org]{link_cross_org()}} result).}
}
\value{
A list: \code{n_new_persons} (persons in \code{new} absent from \code{prior} -- the
wave arrivals), \code{newly_interlocking_persons} (returning persons whose org
span rose from <=1 to >1), \code{persons_with_grown_span} (returning persons whose
\code{XORG_N_ORGS} increased at all), \code{new_interlock_clusters} (multi-org
\code{XORG_ID}s not multi-org before), and the interlock counts before/after.
}
\description{
Compares a prior and a refreshed cross-org assignment (both carrying \code{EMP_ID},
\code{XORG_ID}, \code{XORG_N_ORGS}) and summarizes what a new wave changed. Because
\code{XORG_ID} is now anchored (\code{\link[=anchor_xorg_id]{anchor_xorg_id()}}), a cluster keeps its id as it
grows, so ids compare directly across the two runs.
}
\seealso{
\code{\link[=refresh_cross_org]{refresh_cross_org()}}
}
\keyword{internal}
