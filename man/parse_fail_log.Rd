% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag_links.R
\name{parse_fail_log}
\alias{parse_fail_log}
\title{Record-level parser-defect log for peopleparser}
\usage{
parse_fail_log(
  df,
  emp_id = "EMP_ID",
  org_id = "ein",
  org_name = "org.name",
  year = "taxyr",
  raw_name = "name.raw",
  first = "first_name",
  last = "last_name",
  middle = "middle_name",
  suffix = "suffix",
  salutation = "salutation",
  object_id = "OBJECTID",
  table_id = "TABLE_ID",
  hon = honorific_tokens(),
  cred = credential_tokens(),
  path = NULL
)
}
\arguments{
\item{df}{A linked panel: the output of \code{\link[=link_panel]{link_panel()}} (or any frame carrying
the person id, org, year, raw name, and parsed given/surname columns).}

\item{emp_id, org_id, org_name, year}{Column names for the cross-year person id,
organization id, organization name, and tax year.}

\item{raw_name, first, last}{Column names for the raw (pre-parse) name string and
the parsed given and surname components.}

\item{middle, suffix, salutation}{Optional parsed-component columns, echoed into
the log when present so a fix can be verified against the full parse.}

\item{object_id, table_id}{Optional source-key columns (see \code{\link[=person_year_id]{person_year_id()}});
echoed as the record key when present.}

\item{hon, cred}{Token sets for honorific-glue and credential detection; see
\code{\link[=honorific_tokens]{honorific_tokens()}} and \code{\link[=credential_tokens]{credential_tokens()}}.}

\item{path}{Optional file path; when given, the log is written there with
\code{\link[utils:write.table]{utils::write.csv()}} and returned invisibly.}
}
\value{
A data frame with one row per defective record: the record key, the
\code{raw_name} input, the parsed components, \code{defect_type}, \code{evidence},
\code{expected_first}/\code{expected_last} (\code{NA} where not inferable), and \code{source}
(\code{"self-evident"} or \code{"consensus"}). Rows are unique per record even when a
record trips more than one detector (the defects are concatenated).
}
\description{
Scans a panel for records whose parsed name components bear a \emph{syntactic}
defect that upstream name parsing should fix, and emits one row per offending
record with the raw input, the wrong output, and -- where it can be inferred --
the expected value. These input/wrong/expected triples are directly usable as
\pkg{peopleparser} regression fixtures.
}
\details{
Three defect types are detected:
\describe{
\item{\code{honorific_glue}}{a honorific fused onto the given name (\code{"MRWILLIAM"}
-> expected \code{"WILLIAM"}). Self-evident per record.}
\item{\code{credential_in_surname}}{post-nominal credential tokens inside the
surname (\code{"RM RDMS RVT CRA MCNEIL"} -> expected \code{"MCNEIL"}). Self-evident
per record; the expected surname is the non-credential remainder.}
\item{\code{surname_letter_glue}}{a single stray letter fused onto the surname
(\code{"WSMITH"}). Detected only by \emph{consensus} -- another record of the same
person carries the clean surname (\code{"SMITH"}), which becomes the expected
value -- so this type requires a linked, multi-record person.}
}

Unlike \code{\link[=flag_links]{flag_links()}}, which only inspects clusters whose members disagree,
the self-evident defects are scanned across every record, so a defect that is
consistent across a person's years (and therefore never trips a link flag) is
still reported.
}
\examples{
\dontrun{
log <- parse_fail_log(linked, path = "peopleparser_parse_fails.csv")
table(log$defect_type)
}
}
\seealso{
\code{\link[=flag_links]{flag_links()}}, \code{\link[=honorific_tokens]{honorific_tokens()}}, \code{\link[=credential_tokens]{credential_tokens()}}
}
