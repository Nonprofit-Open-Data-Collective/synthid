% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transitions.R
\name{flag_position_transitions}
\alias{flag_position_transitions}
\title{Flag position transitions -- when the person holding a title changes}
\usage{
flag_position_transitions(
  df,
  position = NULL,
  role = NULL,
  cols = synthid_cols(),
  emp_id = "EMP_ID",
  title = "title.standard",
  match = c("exact", "regex", "fuzzy"),
  min_sim = 0.9,
  gap_tolerant = FALSE,
  overrides = NULL
)
}
\arguments{
\item{df}{A linked panel: output of \code{\link[=link_panel]{link_panel()}}, one record per
organization-year per person, carrying \code{EMP_ID}.}

\item{position}{Character vector of standardized titles (or \code{NULL}).}

\item{role}{Character vector of coarse roles -- \code{"BOARD"}, \code{"OFFICER"},
\code{"STAFF"}, \code{"OTHER"} (or \code{NULL}). At least one of \code{position}/\code{role} is required.}

\item{cols}{Column mapping; see \code{\link[=synthid_cols]{synthid_cols()}} (uses \code{org_id}, \code{year}, \code{name}).}

\item{emp_id}{Person-cluster id column (default \code{"EMP_ID"}).}

\item{title}{Standardized-title column (default \code{"title.standard"}).}

\item{match}{How \code{position} is matched against \code{title}: \code{"exact"} (normalized
equality, the default), \code{"regex"} (alternation), or \code{"fuzzy"} (Jaro-Winkler
at \code{min_sim}).}

\item{min_sim}{Similarity floor for \code{match = "fuzzy"}.}

\item{gap_tolerant}{If \code{TRUE}, measure succession against the last \strong{occupied}
seat-year rather than the prior filing year, so a seat refilled after a
vacancy counts as a transition (default \code{FALSE}).}

\item{overrides}{Passed to \code{\link[=classify_title_role]{classify_title_role()}} when selecting by \code{role}.}
}
\value{
A data frame of class \code{synthid_transitions}, one row per
(organization, filing-year, holder) of the position, ordered by org then year:
\describe{
\item{org id, year (named per \code{cols}), \code{EMP_ID}, \code{name}, \code{title}}{The holder
and the matched standardized title.}
\item{\code{prev_year}}{The reference year succession is measured against -- the
prior filing year (strict) or the last occupied seat-year (gap-tolerant);
\code{NA} for a seat's first occupant.}
\item{\code{years_since_prev}}{\code{year - prev_year}; \code{1} in the strict case unless a
filing gap intervenes, larger when a gap-tolerant match bridges vacant
years.}
\item{\code{n_holders}, \code{n_prev_holders}}{Seat occupancy this and the reference
year.}
\item{\code{incoming}}{Holder did not hold the seat in the reference year.}
\item{\code{continuing}}{Holder also held it in the reference year.}
\item{\code{transition}}{\code{incoming} \strong{and} the seat had a different occupant in
the reference year -- a genuine handover.}
\item{\code{predecessor}}{On a transition row, the \code{EMP_ID}(s) present in the
reference year but gone this year (the person(s) replaced), \verb{;}-joined;
\code{NA} otherwise.}
}
A run summary is attached as attribute \code{"synthid_transitions"}.
}
\description{
For a position that is normally held by one or two people (a CEO seat, a board
presidency), tracks \emph{who} holds it over an organization's filing years and flags
the years where a \strong{new} \code{EMP_ID} takes over from a different holder. This is
the succession signal: the title stays put, the person underneath it changes.
}
\details{
What counts as "the previous year" is set by \code{gap_tolerant}:
\describe{
\item{\code{FALSE} (default, strict)}{The organization's filing timeline (every year
the org appears in \code{df}) defines the previous year. An incoming holder is a
\emph{transition} only when the seat was actually occupied by someone else in the
prior \strong{filing} year; a holder appearing after a vacant seat-year (or the
first time the org reports the position) is an establishment -- \code{incoming}
but not \code{transition}. Use this when a vacancy should not be read as a
succession.}
\item{\code{TRUE} (gap-tolerant)}{The previous year is the most recent year the seat
was actually \strong{occupied}, however many vacant or non-filing years intervene.
A different person refilling a seat that sat empty for years is then a
transition, its predecessor the last person who held it. Use this to follow a
seat across gaps. \code{years_since_prev} reports the span bridged.}
}
Either way, the very first occupant of a seat is \code{incoming} but never a
\code{transition} (no prior holder to succeed).

Select the position by \code{position} (one or more standardized-title strings) and/or
\code{role} (one or more coarse roles from \code{\link[=classify_title_role]{classify_title_role()}}); the two combine
with OR. For a one-or-two-holder seat exact title matching is usually what you
want; \code{role = "BOARD"} selects the whole body (better measured with
\code{\link[=position_churn]{position_churn()}}).
}
\examples{
\dontrun{
linked <- link_panel(panel)
tr <- flag_position_transitions(linked, position = "EXECUTIVE DIRECTOR")
subset(tr, transition)                       # just the handovers
flag_position_transitions(linked, position = c("CEO", "CHIEF EXECUTIVE OFFICER"))
}
}
\seealso{
\code{\link[=position_churn]{position_churn()}} for body-level turnover, \code{\link[=flag_promotions]{flag_promotions()}} for
a person changing title.
}
