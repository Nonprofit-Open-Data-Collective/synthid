% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag_links.R
\name{flag_links}
\alias{flag_links}
\title{Flag questionable cross-year person links for review}
\usage{
flag_links(
  df,
  emp_id = "EMP_ID",
  org_id = "ein",
  org_name = "org.name",
  year = "taxyr",
  raw_name = "name.raw",
  first = "first_name",
  last = "last_name",
  divergence_lv = 0.15,
  divergence_jw = 0.15,
  last_sim_floor = 0.85,
  first_sim_floor = 0.85,
  hon = honorific_tokens(),
  cred = credential_tokens()
)
}
\arguments{
\item{df}{A linked panel: the output of \code{\link[=link_panel]{link_panel()}} (or any frame carrying
the person id, org, year, raw name, and parsed given/surname columns).}

\item{emp_id, org_id, org_name, year}{Column names for the cross-year person id,
organization id, organization name, and tax year.}

\item{raw_name, first, last}{Column names for the raw (pre-parse) name string and
the parsed given and surname components.}

\item{divergence_lv, divergence_jw}{Raw-name divergence thresholds above which a
cluster is flagged (normalised Levenshtein and Jaro-Winkler distance).}

\item{last_sim_floor, first_sim_floor}{Similarity floors (via
\link{compare_last_names}/\link{compare_first_names}) at or above which surnames or
given names are treated as agreeing when assigning a category.}

\item{hon, cred}{Token sets for honorific-glue and credential detection; see
\code{\link[=honorific_tokens]{honorific_tokens()}} and \code{\link[=credential_tokens]{credential_tokens()}}.}
}
\value{
A data frame with one row per multi-record person, class
\code{"synthid_link_flags"}, ordered with the review queue first. Key columns:
\code{category}, \code{review_flag}, \code{review_score}, the per-criterion flags
(\code{flag_first_disagree}, \code{flag_last_disagree}, \code{flag_name_divergence}), the
agreement metrics (\code{last_sim}, \code{first_sim}, \code{name_lv_norm}, \code{name_jw}), and
the distinct values seen (\code{names_seen}, \code{first_seen}, \code{last_seen}). A summary
(thresholds, person and flag counts, category tally) is attached as the
\code{"synthid_flags"} attribute.
}
\description{
Scores every \emph{multi-record} person cluster produced by \code{\link[=link_panel]{link_panel()}} on how
consistent its member records are from year to year, and sorts each flagged
cluster into one primary explanation. The intent is triage: separate the small
set of genuinely questionable links (same surname, materially different given
name, or a near-miss surname that may be two different people) from the large
set whose apparent disagreement has a benign, mechanical cause (a nickname, a
maiden/compound surname, a field-order swap, or an upstream parser artifact).
}
\details{
A cluster is \emph{flagged} when any of three criteria trip: the parsed first names
disagree, the parsed last names disagree, or the raw name strings diverge
beyond \code{divergence_lv} (normalised Levenshtein) or \code{divergence_jw}
(Jaro-Winkler). Every flagged cluster is then assigned, in priority order, to
the first matching \code{category}:
\describe{
\item{\code{order_swap}}{two raw names share the same tokens in different order
(e.g. \code{"SMITH BRAD"} vs \code{"BRAD SMITH"}) -- a correct link.}
\item{\code{parser_honorific_glue}}{a honorific is fused onto the given name
(\code{"MRWILLIAM"}) or a single letter onto the surname (\code{"WSMITH"}) -- a
correct link exposing an upstream parser defect (see \code{\link[=parse_fail_log]{parse_fail_log()}}).}
\item{\code{surname_credential}}{post-nominal credentials leaked into the surname
(\code{"MCNEIL"} vs \code{"RM RDMS RVT CRA MCNEIL"}) -- also a parser defect.}
\item{\code{surname_compound}}{the surnames reconcile as a maiden/hyphenated or
reordered compound (\code{compare_last_names} \eqn{\ge} \code{last_sim_floor}).}
\item{\code{nickname_or_spelling}}{surnames agree and the given names agree as a
nickname, initial, or minor spelling variant (\link{compare_first_names}
\eqn{\ge} \code{first_sim_floor}) -- a correct link.}
\item{\code{review}}{residual: nothing benign explains the divergence. This is the
human queue; see \code{\link[=link_review_queue]{link_review_queue()}}.}
}
}
\examples{
\dontrun{
linked <- link_panel(panel)
flags  <- flag_links(linked)
table(flags$category)
review <- link_review_queue(flags)
}
}
\seealso{
\code{\link[=link_review_queue]{link_review_queue()}} for the residual queue, \code{\link[=parse_fail_log]{parse_fail_log()}} for
the parser-defect handoff.
}
