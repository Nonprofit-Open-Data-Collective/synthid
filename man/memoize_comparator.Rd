% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memoize.R
\name{memoize_comparator}
\alias{memoize_comparator}
\title{Memoize an element-wise pair comparator}
\usage{
memoize_comparator(fn)
}
\arguments{
\item{fn}{An element-wise comparator, \verb{function(x, y) -> numeric}.}
}
\value{
A function with the same contract as \code{fn}, backed by a per-instance
cache. \code{NA} inputs and \code{NA} results are cached and returned faithfully.
}
\description{
Wraps a comparator \code{fn(x, y)} (a function taking two equal-length character
vectors and returning a numeric similarity per element) so that each distinct
\verb{(x, y)} value-pair is evaluated at most once for the lifetime of the wrapper.
}
\details{
The cross-year linker compares the \emph{same} surname / first-name strings over and
over: a person present in all 15 years has their name re-compared against every
coworker in each of the \code{choose(15, 2) = 105} year-pairs. The underlying string
work (tokenising, Jaro-Winkler, phonetic keys) is identical every time. Caching
by the raw string pair collapses that repetition to the number of \emph{distinct}
pairs actually seen, which within one organization is small.

The wrapper is a pure accelerator: for any input it returns byte-identical
output to \code{fn}, so a memoized run produces the same edges, clusters, and
\code{EMP_ID}s as an unmemoized one. It only pays off when the wrapper instance is
reused across many calls (as \code{\link[=match_comparators]{match_comparators()}} does within a single
\code{\link[=link_panel]{link_panel()}} run); a fresh wrapper per call would just add overhead.
}
\keyword{internal}
