% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag_links.R
\name{parse_fail_tokens}
\alias{parse_fail_tokens}
\title{Roll a parse-fail log up into candidate title/credential tokens}
\usage{
parse_fail_tokens(log, known = character())
}
\arguments{
\item{log}{A data frame from \code{\link[=parse_fail_log]{parse_fail_log()}}.}

\item{known}{Optional character vector of already-known tokens (e.g.
\code{toupper(peopleparser::known_titles()$abbr)}); each candidate is marked
\code{in_known}. Tokens with \code{in_known == FALSE} are the add-list.}
}
\value{
A data frame, one row per \verb{(token, field)}: \code{token}, \code{field}
(\code{"surname"} or \code{"given"}), \code{defect_type}, \code{n_records}, \code{n_persons},
\code{example} (a raw name showing it), and \code{in_known}. Ordered by \code{in_known}
(novel first) then \code{n_records} descending.
}
\description{
Reduces the record-level \code{\link[=parse_fail_log]{parse_fail_log()}} to the small, structured list a
\pkg{peopleparser} maintainer actually acts on: the distinct tokens that leaked
into a name field, one row each, with frequency and an example. Tokens are
recovered structurally from the log's \code{last_name}/\code{expected_last} and
\code{first_name}/\code{expected_first} columns (the set-difference of the wrong parse
and the expected value) -- no text parsing of the evidence string.
}
\details{
The \code{credential_in_surname} rows yield the post-nominal tokens that belong in
the prefix/suffix strip list and \code{\link[=known_titles]{known_titles()}}; the \code{honorific_glue} rows
yield the honorific that failed to split (typically already known -- the defect
is tokenisation, not a missing token, so \code{in_known} will be \code{TRUE}).
\code{surname_letter_glue} contributes nothing (a stray initial is not a title).
}
\examples{
\dontrun{
log <- parse_fail_log(linked)
# pass the live peopleparser list as `known` to flag which tokens are novel:
# known <- toupper(peopleparser::known_titles()$abbr)
toks <- parse_fail_tokens(log, known = c("MR", "DR", "MD", "PHD"))
subset(toks, !in_known)          # the tokens to add upstream
}
}
\seealso{
\code{\link[=parse_fail_log]{parse_fail_log()}}
}
