% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/title_role.R
\name{title_role_coverage}
\alias{title_role_coverage}
\title{Audit the role assignment for a title vocabulary}
\usage{
title_role_coverage(titles, overrides = NULL)
}
\arguments{
\item{titles}{Character vector of raw (possibly repeated) titles.}

\item{overrides}{Passed through to \code{\link[=classify_title_role]{classify_title_role()}}.}
}
\value{
A data frame: \code{title} (distinct normalized value), \code{n} (count),
\code{role}, ordered by descending \code{n}.
}
\description{
Tabulates a set of raw titles, classifies each distinct value, and returns the
assignment sorted by frequency -- the tool for reviewing the full ~250-value
vocabulary and finding the tail that lands in \code{OTHER} (or is misclassified)
before pinning exceptions with the \code{overrides} argument of
\code{\link[=classify_title_role]{classify_title_role()}}.
}
\examples{
\dontrun{
cov <- title_role_coverage(panel$title.standard)
subset(cov, role == "OTHER")           # the tail to review
aggregate(n ~ role, cov, sum)          # role mass
}
}
