% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link.R
\name{prepare_panel}
\alias{prepare_panel}
\title{Prepare a raw panel for linkage}
\usage{
prepare_panel(df, cols = synthid_cols())
}
\arguments{
\item{df}{Raw panel data frame.}

\item{cols}{Column mapping (see \code{\link[=synthid_cols]{synthid_cols()}}).}
}
\value{
A list with \code{work} (the cleaned working frame, including \code{.row_uid} and
\code{.surname_w}) and \code{id_string} (the human-readable per-record string).
}
\description{
Validates required columns, normalizes name/attribute fields (upper-cases and
trims), converts blanks in optional fields to \code{NA} so absence is treated as
neutral rather than as agreement, maps an unknown gender (\code{"U"}) to \code{NA}, and
attaches a unique per-record key (\code{.row_uid}) plus the per-record surname
weight.
}
\keyword{internal}
