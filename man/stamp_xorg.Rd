% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link_cross_org.R
\name{stamp_xorg}
\alias{stamp_xorg}
\title{Stamp cross-org person ids back onto the record-level panel}
\usage{
stamp_xorg(
  df,
  x,
  emp_id = "EMP_ID",
  cols = c("XORG_ID", "XORG_N_ORGS", "XORG_N_TITLES", "XORG_N_YEARS", "XORG_N_RECORDS")
)
}
\arguments{
\item{df}{The record-level panel that was linked (carries \code{EMP_ID}).}

\item{x}{Either the \code{assignment} data frame from \code{\link[=link_cross_org]{link_cross_org()}} or the full
\code{link_cross_org()} result list (its \verb{$assignment} is used).}

\item{emp_id}{Name of the person-cluster id column (default \code{"EMP_ID"}).}

\item{cols}{Cross-org columns to copy over; defaults to all present in the
assignment (\code{XORG_ID}, \code{XORG_N_ORGS}, \code{XORG_N_TITLES}, \code{XORG_N_YEARS},
\code{XORG_N_RECORDS}).}
}
\value{
\code{df} with the requested cross-org columns added. Rows whose \code{EMP_ID} is
absent from the assignment get \code{NA} (and a warning).
}
\description{
Joins the cross-org \code{assignment} (\code{\link[=link_cross_org]{link_cross_org()}}) onto the original
record-level panel by \code{EMP_ID}, so every filing row of a real person -- across
every organization and tax year -- carries one \code{XORG_ID}. Grouping the stamped
panel by \code{XORG_ID} then recovers a person's complete title/year history across
organizations (grouping by \code{EMP_ID} gives it within a single organization).
}
\examples{
\dontrun{
xo <- link_cross_org(linked)
panel <- stamp_xorg(linked, xo)
# a person's full cross-org title history:
subset(panel, XORG_ID == panel$XORG_ID[1])[, c("ein","taxyr","title.standard")]
}
}
\seealso{
\code{\link[=link_cross_org]{link_cross_org()}}
}
