% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anchor.R
\name{anchor_xorg_id}
\alias{anchor_xorg_id}
\title{Mint an anchored cross-org XORG_ID from a founding member's EMP_ID}
\usage{
anchor_xorg_id(anchor_emp_id, keyspec = "v1")
}
\arguments{
\item{anchor_emp_id}{Character vector of anchor \code{EMP_ID}s (one per cluster).}

\item{keyspec}{Version tag baked into the hashed string.}
}
\value{
A character vector of \code{XORG-...} ids.
}
\description{
The cross-org analogue of \code{\link[=anchor_emp_id]{anchor_emp_id()}}: hashes the anchor member's
(already-stable) \code{EMP_ID} into an \code{XORG-...} interlock id. Anchoring the id to
one founding member -- rather than to the whole member set -- means adding a
person to an interlock (e.g. on a cross-org refresh after a new wave) does not
change the cluster's \code{XORG_ID}, the same stability \code{\link[=anchor_emp_id]{anchor_emp_id()}} gives at
the person level. A scheme tag keeps these off the legacy hash-of-membership
\code{XORG_ID} keyspace.
}
\seealso{
\code{\link[=anchor_emp_id]{anchor_emp_id()}}, \code{\link[=resolve_cross_org]{resolve_cross_org()}}
}
