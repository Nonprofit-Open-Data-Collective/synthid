% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_edges.R
\name{filter_edges_by_role}
\alias{filter_edges_by_role}
\title{Filter cross-organization edges by the role composition of their endpoints}
\usage{
filter_edges_by_role(
  edges,
  profiles,
  target = "BOARD",
  mode = c("any", "all", "primary"),
  match = c("both", "either"),
  negate = FALSE,
  annotate = TRUE
)
}
\arguments{
\item{edges}{Edge table with \code{emp_a}, \code{emp_b} (e.g. \code{link_cross_org()$edges}).}

\item{profiles}{Profiles those ids index (\code{\link[=build_person_profile]{build_person_profile()}}); needs
\code{roles} and \code{primary_role}.}

\item{target}{Role class(es) to test against, from \code{BOARD}/\code{OFFICER}/\code{STAFF}/
\code{OTHER}.}

\item{mode}{How to reduce an endpoint's role set: \code{"any"}, \code{"all"}, \code{"primary"}.}

\item{match}{Require the endpoint test on \code{"both"} ends or \code{"either"}.}

\item{negate}{Match endpoints \emph{not} satisfying the target test.}

\item{annotate}{Append \code{roles_a}, \code{roles_b} (the endpoints' role sets, \code{+}-
joined) to the result for inspection.}
}
\value{
The subset of \code{edges} whose endpoints satisfy the predicate (with the
annotation columns when \code{annotate}).
}
\description{
Selects cross-org candidate/accepted edges (\code{\link[=score_candidate_pairs]{score_candidate_pairs()}},
\code{\link[=link_cross_org]{link_cross_org()}}) by the coarse role (\code{\link[=classify_title_role]{classify_title_role()}}) each endpoint
held. Each edge endpoint is one within-org \code{EMP_ID} -- one person's whole
tenure at one organization -- so an endpoint's role evidence is the \emph{set} of
roles it held across the years of that tenure (\code{profiles$roles}). This is a
within-tenure union, \strong{not} a union across organizations (the other org is the
other endpoint).
}
\details{
Because a within-tenure role set can mix roles (a paid director who later took
a board seat), \code{mode} controls how the set is reduced to a yes/no per endpoint:
\describe{
\item{\code{any}}{Endpoint matches if it held \emph{any} role in \code{target} (the
union/"ever" behaviour -- same as \code{profiles$is_board} for \code{target="BOARD"}).}
\item{\code{all}}{Endpoint matches only if \emph{every} role it held is in \code{target}.}
\item{\code{primary}}{Test only the modal role (\code{profiles$primary_role}).}
}
\code{negate = TRUE} flips the per-endpoint test (matches endpoints \emph{not} in
\code{target}); combined with \code{mode = "any"} it means "never held a \code{target} role",
the strict complement. \code{match} then requires the test to hold for \code{both}
endpoints or \code{either}. An endpoint with no recorded role is treated as not in
\code{target}.
}
\examples{
\dontrun{
xo <- link_cross_org(linked)
# strictly-paid interlocks (officer/staff only, no board, no volunteer OTHER):
filter_edges_by_role(xo$edges, xo$profiles,
                     target = c("OFFICER","STAFF"), mode = "all")
}
}
\seealso{
\code{\link[=board_only_edges]{board_only_edges()}}, \code{\link[=non_board_edges]{non_board_edges()}}
}
