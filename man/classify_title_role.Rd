% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/title_role.R
\name{classify_title_role}
\alias{classify_title_role}
\title{Assign a coarse role to standardized titles}
\usage{
classify_title_role(title, overrides = NULL, levels = TRUE)
}
\arguments{
\item{title}{Character vector of titles (any case; whitespace is squished).}

\item{overrides}{Optional named character vector mapping an \emph{exact} normalized
title to a role, e.g. \code{c("SERGEANT AT ARMS" = "BOARD")}. Overrides win over
every rule, so this is the one-line escape hatch for tail cases the rules get
wrong. Names are matched after the same normalization applied to \code{title}.}

\item{levels}{If \code{TRUE} (default) return a factor with levels
\code{c("BOARD","OFFICER","STAFF","OTHER")}; otherwise a character vector.}
}
\value{
A factor (or character vector) the same length as \code{title}; \code{NA} titles
map to \code{NA}.
}
\description{
Vectorized classifier mapping each \code{title.standard} value to one of
\code{BOARD}, \code{OFFICER}, \code{STAFF}, \code{OTHER} (see \link{title_role}). Precedence is
overrides, then \code{BOARD}, then \code{OFFICER}, then \code{STAFF}, then \code{OTHER}.
}
\examples{
classify_title_role(c("BOARD PRESIDENT", "BOARD SECRETARY", "TREASURER",
                       "CHIEF FINANCIAL OFFICER", "EXECUTIVE DIRECTOR",
                       "VICE PRESIDENT", "VICE PRESIDENT OF FINANCE",
                       "DIRECTOR OF MARKETING", "CHAPLAIN"))
}
\seealso{
\code{\link[=title_role_coverage]{title_role_coverage()}} to audit a whole vocabulary.
}
