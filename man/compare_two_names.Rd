% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/names.R
\name{compare_two_names}
\alias{compare_two_names}
\title{Similarity between two (possibly compound) surnames}
\usage{
compare_two_names(x, y, floor = 0.8, rescale = 0.5)
}
\arguments{
\item{x, y}{Single character strings (each may be compound/hyphenated).}

\item{floor}{Minimum token-pair similarity counted as a match in the coverage
view.}

\item{rescale}{Lower anchor for the final rescaling; similarities at or below
it map to 0.}
}
\value{
A similarity in \verb{[0, 1]} (\code{1} = identical); \code{NA_real_} if either name is
missing or empty.
}
\description{
Returns the maximum of two views, which together are robust to the two ways a
parser mangles a surname without changing the person:
\describe{
\item{whole-string}{Jaro-Winkler on the names with all separators removed.
Rescues separator noise where token splitting fails --- \code{"MCLANE"} vs
\code{"MC-LANE"} collapse to the same string (1.0), and \code{"VANDERBERG"} vs
\code{"VAN DER BERG"} likewise.}
\item{coverage token-match}{Greedy one-to-one Jaro-Winkler matching of the
tokens, summing matched similarities and dividing by the \emph{smaller} token
count. Rescues a dropped or reordered token --- \code{"ANDREWS-MCLANE"} vs
\code{"MCLANE"} is 1.0 because the shorter name is fully covered --- while
penalising two different multi-token surnames that merely share one token:
\code{"WEINER-COHEN"} vs \code{"COHEN-GANTSOUDES"} scores low, not 1.0.}
}
The earlier "best single token" rule returned 1.0 for the latter case, letting
two different people who share one surname token look identical; dividing by
the smaller token count is what closes that leak.
}
\details{
The max of the two views is finally rescaled by \code{(v - rescale) / (1 - rescale)}
so that unrelated surnames (Jaro-Winkler rarely drops below ~0.5) land near 0
and penalise in scoring, instead of reading as "half a match".
}
\examples{
compare_two_names("ANDREWS-MCLANE", "MCLANE")        # dropped token -> ~1
compare_two_names("WEINER-COHEN", "COHEN-GANTSOUDES") # shared token only -> low
}
