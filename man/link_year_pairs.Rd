% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link.R
\name{link_year_pairs}
\alias{link_year_pairs}
\title{Link records across every pair of years within organizations}
\usage{
link_year_pairs(
  work,
  weights = default_weights(),
  threshold = 7,
  verbose = FALSE
)
}
\arguments{
\item{work}{Prepared working frame from \code{\link[=prepare_panel]{prepare_panel()}}.}

\item{weights}{Named base weights (see \code{\link[=default_weights]{default_weights()}}).}

\item{threshold}{Minimum score for a candidate to be eligible.}

\item{verbose}{Print per-year-pair progress.}
}
\value{
A data frame of accepted edges: \code{row_x}, \code{row_y} (the \code{.row_uid}s),
\code{score}, \code{yr_x}, \code{yr_y}.
}
\description{
For each ordered pair of tax years, blocks candidate pairs on organization,
compares the configured name fields, scores each candidate with the
surname-frequency adjustment, keeps candidates at or above \code{threshold}, and
enforces a one-to-one assignment within the year pair (greedy, highest score
first). Comparing all year pairs -- not just consecutive ones -- lets a person
absent in the middle year still be linked across the gap.
}
\keyword{internal}
