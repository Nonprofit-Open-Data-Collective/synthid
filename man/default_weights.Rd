% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score.R
\name{default_weights}
\alias{default_weights}
\title{Default component weights for the person-match score}
\usage{
default_weights()
}
\value{
A named numeric vector of base weights.
}
\description{
Base weights for each name/attribute field. The final contribution of a field
is \code{weight * (2 * similarity - 1)}, so an identical field adds \code{+weight}, a
completely different field subtracts \code{weight}, and a missing field on either
side contributes \code{0} (neutral). The \code{last_name} weight is additionally scaled,
per pair, by a within-organization surname-rarity factor (see
\code{\link[=surname_weight]{surname_weight()}}): on a family-dominated board, agreement on the shared
surname carries almost no weight and the first name / suffix / gender do the
discriminating.
}
