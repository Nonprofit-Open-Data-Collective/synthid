% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anchor.R
\name{remint_anchored}
\alias{remint_anchored}
\title{Re-mint a linked panel's EMP_IDs under the anchored scheme}
\usage{
remint_anchored(df, cols = synthid_cols(), emp_id = "EMP_ID")
}
\arguments{
\item{df}{A linked panel carrying \code{emp_id} plus the organization, year, and
name columns named in \code{cols} (and, ideally, the native \code{OBJECTID}/\code{TABLE_ID}
key columns for durable anchoring).}

\item{cols}{Column mapping; see \code{\link[=synthid_cols]{synthid_cols()}}.}

\item{emp_id}{Name of the existing person-id column to re-mint (default
\code{"EMP_ID"}).}
}
\value{
A list with \code{panel} (\code{df} with \code{EMP_ID} re-minted and \code{EMP_ANCHOR}
added) and \code{crosswalk} (\code{old_emp_id}, \code{new_emp_id}, \code{emp_anchor}) so
downstream tables keyed on the old ids can follow the rename.
}
\description{
One-time migration for a panel linked under the legacy hash-of-membership id
recipe. Cluster membership is taken as-is from the existing \code{EMP_ID} column
(this does \strong{not} re-link), and each cluster is re-stamped with an anchored id
(\code{\link[=emp_anchor_key]{emp_anchor_key()}} -> \code{\link[=anchor_emp_id]{anchor_emp_id()}}) plus its \code{EMP_ANCHOR} key. Because
clustering is untouched, the result is identical to what a fresh anchored
\code{\link[=link_panel]{link_panel()}} run would produce for the same data.
}
\seealso{
\code{\link[=emp_anchor_key]{emp_anchor_key()}}, \code{\link[=anchor_emp_id]{anchor_emp_id()}}, \code{\link[=link_panel]{link_panel()}}
}
