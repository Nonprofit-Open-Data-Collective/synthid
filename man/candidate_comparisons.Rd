% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{candidate_comparisons}
\alias{candidate_comparisons}
\title{All candidate pairs with their per-field comparison vectors}
\usage{
candidate_comparisons(df, cols = synthid_cols(), features = NULL)
}
\arguments{
\item{df}{A raw panel (as for \code{\link[=link_panel]{link_panel()}}).}

\item{cols}{Column mapping; see \code{\link[=synthid_cols]{synthid_cols()}}.}

\item{features}{Feature columns to compare (defaults to the standard set).}
}
\value{
A data frame, one row per candidate pair: \code{row_x}, \code{row_y}, \code{yr_x},
\code{yr_y}, \code{org}, \code{surname_w_x}, \code{surname_w_y}, and one numeric similarity column
per feature (\code{NA} where a field was missing on either side).
}
\description{
Like \code{\link[=candidate_scores]{candidate_scores()}}, but returns the raw per-field similarity columns
(the graded comparison vector) rather than a single combined score. This is the
substrate for fitting a match model (\code{\link[=fit_match_model]{fit_match_model()}}).
}
