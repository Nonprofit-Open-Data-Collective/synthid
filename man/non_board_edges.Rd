% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_edges.R
\name{non_board_edges}
\alias{non_board_edges}
\title{Non-board (paid-employee) edges}
\usage{
non_board_edges(
  edges,
  profiles,
  mode = c("any", "all", "primary"),
  match = c("both", "either"),
  annotate = TRUE
)
}
\arguments{
\item{edges}{Edge table with \code{emp_a}, \code{emp_b} (e.g. \code{link_cross_org()$edges}).}

\item{profiles}{Profiles those ids index (\code{\link[=build_person_profile]{build_person_profile()}}); needs
\code{roles} and \code{primary_role}.}

\item{mode}{How to reduce an endpoint's role set: \code{"any"}, \code{"all"}, \code{"primary"}.}

\item{match}{Require the endpoint test on \code{"both"} ends or \code{"either"}.}

\item{annotate}{Append \code{roles_a}, \code{roles_b} (the endpoints' role sets, \code{+}-
joined) to the result for inspection.}
}
\value{
The non-board subset of \code{edges}.
}
\description{
Convenience wrapper for \code{\link[=filter_edges_by_role]{filter_edges_by_role()}} keeping edges where neither
endpoint held a board role -- the population for questions about \emph{employment}
rather than governance (e.g. do paid staff get promoted faster by moving
organizations or by staying?). With the default \code{mode = "any"} an endpoint
qualifies only if it \emph{never} held a board seat at that org (strict paid-only);
\code{mode = "primary"} relaxes this to "board was not their dominant role".
}
\details{
Note this keeps \code{OFFICER}, \code{STAFF}, \strong{and} \code{OTHER} (non-board volunteer roles
such as \code{CHAPLAIN}). For strictly \emph{paid} employees, filter positively instead:
\code{filter_edges_by_role(edges, profiles, target = c("OFFICER","STAFF"), mode = "all")}.
}
\examples{
\dontrun{ non_board_edges(link_cross_org(linked)$edges, profiles) }
}
