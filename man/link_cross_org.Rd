% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link_cross_org.R
\name{link_cross_org}
\alias{link_cross_org}
\title{Cross-organization person linkage (interlock) pipeline}
\usage{
link_cross_org(
  df,
  cols = synthid_cols(),
  state = NULL,
  ntee = NULL,
  passes = names(blocking_passes()),
  weights = default_weights(),
  threshold = 7,
  max_block_size = 5000L,
  verbose = FALSE
)
}
\arguments{
\item{df}{A linked panel (output of \code{\link[=link_panel]{link_panel()}}).}

\item{cols}{Column mapping; see \code{\link[=synthid_cols]{synthid_cols()}}.}

\item{state, ntee}{Optional organization geography / NTEE columns for the tighter
blocking passes (join from the BMF beforehand). \code{NULL} -> those passes emit
nothing and linkage falls back to the surname pass.}

\item{passes}{Blocking passes to run; see \code{\link[=blocking_passes]{blocking_passes()}}.}

\item{weights}{Base match weights; see \code{\link[=default_weights]{default_weights()}}.}

\item{threshold}{Minimum score to accept a cross-org edge (default \code{7}, the
within-org default; \strong{needs its own tuning} -- the score scale differs, as
title/salutation are dropped and surname rarity is population-based).}

\item{max_block_size}{Oversize-bucket guard for \code{\link[=candidate_pairs]{candidate_pairs()}}.}

\item{verbose}{Print stage sizes.}
}
\value{
A list: \code{profiles} and \code{assignment} both carry the cross-org rollups
\code{XORG_ID}, \code{XORG_N_ORGS}, \code{XORG_N_TITLES}, \code{XORG_N_YEARS} (distinct calendar
years), \code{XORG_N_RECORDS} (total filing rows); \code{edges} (all scored candidate
pairs); and \code{report} (counts, including edges rejected by the org guard).
Stamp the assignment back onto the panel with \code{\link[=stamp_xorg]{stamp_xorg()}} to recover each
person's full title history.
}
\description{
End-to-end stage-1 + stage-2 cross-org linkage over a panel that already
carries within-org \code{EMP_ID}s (\code{\link[=link_panel]{link_panel()}}): build one profile per person,
generate candidate pairs by hash blocking, score them from the variant sets,
link pairs at or above \code{threshold}, and resolve the accepted edges into
cross-organization person clusters (\code{XORG_ID}) under the one-person-per-org
invariant.
}
\examples{
\dontrun{
linked <- link_panel(panel)
xo <- link_cross_org(linked, state = "state", ntee = "ntee", threshold = 8)
subset(xo$profiles, XORG_N_ORGS > 1)          # people who interlock
subset(xo$edges, score >= 8 & is_board_a & is_board_b)  # board-board interlocks
}
}
\seealso{
\code{\link[=build_person_profile]{build_person_profile()}}, \code{\link[=person_blocking_keys]{person_blocking_keys()}},
\code{\link[=candidate_pairs]{candidate_pairs()}}, \code{\link[=score_candidate_pairs]{score_candidate_pairs()}}
}
