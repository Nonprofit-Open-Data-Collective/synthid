% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross_org.R
\name{person_blocking_keys}
\alias{person_blocking_keys}
\title{Explode person profiles into a long blocking-key table}
\usage{
person_blocking_keys(
  profiles,
  passes = names(blocking_passes()),
  ntee_digits = 1L
)
}
\arguments{
\item{profiles}{Output of \code{\link[=build_person_profile]{build_person_profile()}} (needs \code{last_name_keys};
\code{states}/\code{ntee} list-columns for the geography/industry passes).}

\item{passes}{Character vector naming the passes to emit (see
\code{\link[=blocking_passes]{blocking_passes()}}); default all four.}

\item{ntee_digits}{Number of leading characters of each NTEE code to key on
(default \code{1} = major group, e.g. \code{"B25"} -> \code{"B"}).}
}
\value{
A data frame \code{EMP_ID}, \code{pass}, \code{block_key} (one row per bucket a person
lands in). \code{block_key} is \code{"<pass>|<last>[|<state>][|<ntee>]"}.
}
\description{
Turns each \code{\link[=build_person_profile]{build_person_profile()}} row into the set of hash buckets it belongs
to -- the stage-1 input for cross-organization linkage. Because a profile keeps
its \emph{variant sets}, it emits the Cartesian product of its surname phonetic keys,
states, and (truncated) NTEE codes for each requested pass, so a person with a
maiden and a married surname, or activity in two states, lands in every
matching bucket instead of a single one.
}
\details{
Keys are namespaced by pass, so a \code{strict} bucket and a \code{surname} bucket never
collide even when they share a surname key. A pass that needs a component the
profile lacks (e.g. \code{strict} when \code{states} is empty) simply emits nothing for
that profile.
}
\examples{
\dontrun{
profiles <- build_person_profile(linked, state = "state", ntee = "ntee")
keys <- person_blocking_keys(profiles)
table(keys$pass)
}
}
\seealso{
\code{\link[=candidate_pairs]{candidate_pairs()}} for the hash join over these keys.
}
