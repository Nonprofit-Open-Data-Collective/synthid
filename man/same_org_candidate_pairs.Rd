% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/incremental.R
\name{same_org_candidate_pairs}
\alias{same_org_candidate_pairs}
\title{Same-organization, surname-blocked bipartite candidate pairs}
\usage{
same_org_candidate_pairs(existing, wave, max_block_size = 5000L)
}
\arguments{
\item{existing, wave}{Profiles (\code{\link[=build_person_profile]{build_person_profile()}}) for the frozen panel
and the new wave. Each within-org \code{EMP_ID} is one person in one org (its
\code{orgs} list-column is a singleton).}

\item{max_block_size}{Skip (and report) any \verb{(org, surname-key)} bucket whose
existing x wave product exceeds this, to avoid a common-surname blow-up.}
}
\value{
A data frame of unordered candidate pairs \code{emp_a} (existing), \code{emp_b}
(wave). Oversize buckets skipped are in the \code{"dropped_blocks"} attribute.
}
\description{
Stage-1 blocking for incremental wave matching. Unlike the cross-org blocker
(\code{\link[=candidate_pairs]{candidate_pairs()}}), which crosses organizations and drops same-org pairs,
this keeps only pairs that share an organization -- a wave attaches \emph{within} an
org -- and is bipartite: every pair has one \code{existing} person and one \code{wave}
person (never existing-existing or wave-wave). Two people share a bucket when
they share an organization and a surname phonetic key (\code{last_name_keys}), so
maiden/married/OCR surname variants still block.
}
\keyword{internal}
