% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transitions.R
\name{flag_promotions}
\alias{flag_promotions}
\title{Flag promotions -- when a person's title changes for real}
\usage{
flag_promotions(
  df,
  cols = synthid_cols(),
  emp_id = "EMP_ID",
  title_standard = "title.standard",
  title_raw = "title.raw",
  comp = "tot.comp",
  min_sim = 0.85,
  comp_delta = 0.1,
  overrides = NULL
)
}
\arguments{
\item{df}{A linked panel: output of \code{\link[=link_panel]{link_panel()}}, carrying \code{EMP_ID}.}

\item{cols}{Column mapping; see \code{\link[=synthid_cols]{synthid_cols()}} (uses \code{org_id}, \code{year}, \code{name}).}

\item{emp_id}{Person-cluster id column (default \code{"EMP_ID"}).}

\item{title_standard, title_raw}{Standardized and raw title columns. \code{title_raw}
is optional; when absent the raw view is skipped and only the standardized
title drives classification.}

\item{comp}{Optional compensation column (default \code{"tot.comp"}); when present it
is the primary signal for promotion direction. Absent -> direction falls back
to role rank.}

\item{min_sim}{Jaro-Winkler floor at which two titles count as "the same label"
(default \code{0.85}). Higher = stricter (more changes judged real).}

\item{comp_delta}{Fractional compensation change treated as a real pay move
(default \code{0.10}); within +/- this, pay is called flat and direction defers to
role rank.}

\item{overrides}{Passed to \code{\link[=classify_title_role]{classify_title_role()}} for the role columns.}
}
\value{
A data frame of class \code{synthid_promotions}, one row per title change
(all types), ordered by person then year:
\describe{
\item{\code{EMP_ID}, org id (named per \code{cols}), \code{name}}{The person.}
\item{\code{from_year}, \code{to_year}}{Consecutive observed years across the change.}
\item{\code{from_title}, \code{to_title}, \code{from_title_raw}, \code{to_title_raw}}{The
standardized and raw labels on each side.}
\item{\code{from_role}, \code{to_role}}{Coarse roles (\code{\link[=classify_title_role]{classify_title_role()}}).}
\item{\code{sim_standard}, \code{sim_raw}}{Jaro-Winkler similarity of the two labels.}
\item{\code{from_comp}, \code{to_comp}, \code{comp_change}, \code{comp_pct}}{Compensation move
(present only when \code{comp} is supplied).}
\item{\code{change_type}}{Factor \code{relabel}/\code{oscillation}/\code{transient}/\code{real}.}
\item{\code{direction}}{For \code{real} rows: \code{promotion}/\code{demotion}/\code{lateral}; \code{NA}
otherwise.}
\item{\code{basis}}{What set the direction: \code{"comp"}, \code{"role"}, or \code{"none"}.}
}
A run summary is attached as attribute \code{"synthid_promotions"}.
}
\description{
Walks each person's (\code{EMP_ID}) year-ordered title history and flags the changes
that are genuine role moves, separating them from the noise that makes a naive
"title changed => promotion" rule wrong. Titles are not reported consistently
from year to year, so a raw diff over-counts wildly; this function inspects the
person's \emph{upstream} history and both the raw and standardized labels to sort each
change into one of:
\describe{
\item{\code{relabel}}{The two labels are near-identical under Jaro-Winkler on
\strong{either} the standardized or the raw title (\verb{>= min_sim}) -- a spelling or
formatting variant of the same post, not a move.}
\item{\code{oscillation}}{The "new" title fuzzy-matches a title the person already
held in an \strong{earlier} year (before the prior year) -- the record is
flip-flopping between inconsistent labels, not advancing.}
\item{\code{transient}}{A novel title that lasts a single year and then \strong{reverts}
to the prior title next year -- a one-year labeling blip.}
\item{\code{real}}{A novel title, not a relabel, not seen upstream, that does not
immediately revert. Direction (\code{promotion} / \code{demotion} / \code{lateral}) is read
from the compensation change when present, else from a coarse role rank.}
}
Using both the raw and standardized titles is what catches the case where the
standardizer itself is inconsistent (two standard labels for one raw title, or
the reverse): agreement on \emph{either} view marks the change as a relabel.
}
\examples{
\dontrun{
linked <- link_panel(panel)
pr <- flag_promotions(linked)
subset(pr, change_type == "real" & direction == "promotion")
table(pr$change_type)                        # how much was noise
}
}
\seealso{
\code{\link[=flag_position_transitions]{flag_position_transitions()}}, \code{\link[=classify_title_role]{classify_title_role()}}.
}
