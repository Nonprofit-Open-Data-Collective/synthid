% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/id.R
\name{person_year_id}
\alias{person_year_id}
\title{Build deterministic per-record person-year identifiers from source keys}
\usage{
person_year_id(object_id, table_id, keyspec = "v1")
}
\arguments{
\item{object_id, table_id}{Equal-length character vectors: the raw \code{OBJECTID}
and \code{TABLE_ID} source columns.}

\item{keyspec}{Version tag baked into the hash; identifies the recipe.}
}
\value{
A character vector of \code{PYID-...} identifiers, one per input row.
}
\description{
Hashes the native filing/person key \verb{(OBJECTID, TABLE_ID)} into a stable
\verb{PYID-} identifier. This is the durable back-link that ties a Part VII record
to its source row across the raw, classified, and linked panels -- distinct
from the cross-year cluster id \code{\link[=create_emp_ids]{create_emp_ids()}} stamps as \code{EMP_ID}.
}
\details{
\code{OBJECTID} is parser-independent (it comes from the IRS filing index, not the
XML body parse); \code{TABLE_ID} is the stored per-person key within a filing.
\verb{(OBJECTID, TABLE_ID)} is unique and non-missing across the raw Part VII data,
so unlike a name/title key it needs no disambiguation and is unaffected by
downstream name or title cleaning. Both tokens are drawn from \verb{[A-Z0-9-]}, so
the \code{|} field delimiter below can never occur inside them.

The recipe is frozen under \code{keyspec}; bump it if the inputs or delimiter ever
change, so old and new ids never silently collide.
}
\section{Future work -- CONTENT_ID}{

\code{PERSON_YEAR_ID} depends on \code{TABLE_ID}, which is parser-derived and has broken
on pathological XML in the past. A parser revision that renumbers \code{TABLE_ID}
would shift every id. A parser-independent \code{CONTENT_ID} -- a hash of
normalized native content fields (name variants + raw title + compensation) --
could be stored alongside as a recovery bridge to re-map old<->new ids for the
records whose content is stable. Not implemented yet; note that a parser
change severe enough to break \code{TABLE_ID} for a record would usually corrupt
that record's content extraction too, so \code{CONTENT_ID} mainly insures against a
\emph{global} renumbering of otherwise-clean records. Persisting the raw \code{OBJECTID}
and \code{TABLE_ID} columns (see \verb{dev/01_preprocess_year.R}) keeps that bridge
buildable after the fact.
}

\examples{
person_year_id(c("OID-1", "OID-1"), c("TID-00001", "TID-00002"))
}
