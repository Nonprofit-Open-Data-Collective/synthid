% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross_org.R
\name{candidate_pairs}
\alias{candidate_pairs}
\title{Generate cross-organization candidate pairs by hash join}
\usage{
candidate_pairs(keys, profiles, max_block_size = 5000L)
}
\arguments{
\item{keys}{Long key table from \code{\link[=person_blocking_keys]{person_blocking_keys()}}.}

\item{profiles}{The profiles the keys came from; supplies each person's
organization (from \code{orgs}) for the same-organization filter.}

\item{max_block_size}{Skip (and report) any bucket with more than this many
distinct people (default \code{5000}).}
}
\value{
A data frame, one row per unordered candidate pair: \code{emp_a}, \code{emp_b}
(string-sorted), \code{pass} and \code{pass_rank} of the \emph{tightest} bucket that
produced the pair, and \code{n_shared_buckets} (how many buckets they co-occur in
-- a cheap first-pass strength signal). Attributes: \code{"dropped_blocks"} (over-
size buckets skipped) and \code{"n_comparisons"} (pairs generated, i.e. the
stage-2 workload).
}
\description{
Stage 1 of cross-org linkage: instead of a materialized within-block cross
product (the reclin2 \code{pair_blocking} approach used within an organization),
this groups profiles by hash bucket (\code{\link[=person_blocking_keys]{person_blocking_keys()}}) and emits the
distinct person pairs that co-occur in a bucket. Only these candidates are
handed to the (expensive) stage-2 scorer.
}
\details{
Since each within-organization \code{EMP_ID} denotes one person in one organization,
a genuine interlock links two \code{EMP_ID}s in \emph{different} organizations, so pairs
sharing an organization are dropped. Buckets larger than \code{max_block_size}
(typically a very common surname with geography/industry relaxed away) would
emit O(n^2) mostly-spurious pairs; they are skipped and reported in the
\code{"dropped_blocks"} attribute rather than silently truncated.
}
\examples{
\dontrun{
keys  <- person_blocking_keys(profiles)
cand  <- candidate_pairs(keys, profiles)
attr(cand, "n_comparisons")            # vs choose(nrow(profiles), 2)
}
}
\seealso{
\code{\link[=person_blocking_keys]{person_blocking_keys()}}, \code{\link[=compare_last_names]{compare_last_names()}}, \code{\link[=compare_first_names]{compare_first_names()}}
}
