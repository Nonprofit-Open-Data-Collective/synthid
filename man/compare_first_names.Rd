% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/first_names.R
\name{compare_first_names}
\alias{compare_first_names}
\title{Compare two first names, with nickname, initial, and phonetic handling}
\usage{
compare_first_names(a, b, floor = 0.5)
}
\arguments{
\item{a, b}{Character vectors of equal length.}

\item{floor}{JW rescaling floor (default \code{0.5}).}
}
\value{
Numeric vector of similarities in \verb{[0, 1]}; \code{NA} where a name is
missing.
}
\description{
The comparator used for the \code{first_name} field. Plain Jaro-Winkler is a poor
fit for given names in two ways: it misses nickname pairs (\code{BOB} vs \code{ROBERT}
score low) and it rarely drops below ~0.5 for unrelated names (so a genuinely
different first name adds little discriminating penalty). This comparator fixes
both:
\itemize{
\item exact match -> \code{1};
\item nickname/diminutive of the same canonical (\code{nickname_table()}) -> \code{0.95};
\item single initial vs full name -> \code{0.6} if the initial agrees, else \code{0}
(an initial is weak evidence, neither a match nor a strong mismatch);
\item otherwise Jaro-Winkler rescaled by \code{(jw - floor) / (1 - floor)} so
unrelated names fall toward \code{0} and actually penalise, with a Soundex
agreement raising the result to at least \code{0.5} (a sound-alike is not
penalised, but -- because Soundex over-collapses -- is not asserted as a
match either).
}
}
\examples{
compare_first_names(c("BOB", "ALEXIS", "JON"), c("ROBERT", "MARCELLA", "JONATHAN"))
}
