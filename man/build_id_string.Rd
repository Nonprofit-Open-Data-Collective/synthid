% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/id.R
\name{build_id_string}
\alias{build_id_string}
\title{Build a canonical, human-readable per-record identifier string}
\usage{
build_id_string(
  df,
  org_id = "ein",
  year = "taxyr",
  name = "name",
  title = "title.standard"
)
}
\arguments{
\item{df}{A data frame.}

\item{org_id, year, name, title}{Column names in \code{df} for the organization id, tax
year, person name, and standardized title.}
}
\value{
A unique character vector of length \code{nrow(df)}.
}
\description{
Produces one string per row that encodes the organization, tax year, name, and
standardized title. The string is meant to be human-legible for debugging and
to be fed to \code{\link[=create_emp_ids]{create_emp_ids()}} for hashing. Where two rows produce an identical
string (a genuine duplicate record within an organization-year) a \verb{-D\{k\}}
occurrence suffix is appended so the returned vector is always unique.
}
\details{
This replaces the draft \code{create_hash_string()}, which interpolated the literal
strings \code{"EIN2"} and \code{OID} instead of the corresponding columns and whose
argument names did not match the call site.
}
\examples{
df <- data.frame(
  ein = c("11", "11"), taxyr = c(2019, 2020),
  name = c("Jane Roe", "Jane Roe"), title.standard = c("CEO", "CEO")
)
build_id_string(df)
}
