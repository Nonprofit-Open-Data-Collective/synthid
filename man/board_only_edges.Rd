% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_edges.R
\name{board_only_edges}
\alias{board_only_edges}
\title{Board-governance interlock edges}
\usage{
board_only_edges(
  edges,
  profiles,
  mode = c("any", "all", "primary"),
  match = c("both", "either"),
  annotate = TRUE
)
}
\arguments{
\item{edges}{Edge table with \code{emp_a}, \code{emp_b} (e.g. \code{link_cross_org()$edges}).}

\item{profiles}{Profiles those ids index (\code{\link[=build_person_profile]{build_person_profile()}}); needs
\code{roles} and \code{primary_role}.}

\item{mode}{How to reduce an endpoint's role set: \code{"any"}, \code{"all"}, \code{"primary"}.}

\item{match}{Require the endpoint test on \code{"both"} ends or \code{"either"}.}

\item{annotate}{Append \code{roles_a}, \code{roles_b} (the endpoints' role sets, \code{+}-
joined) to the result for inspection.}
}
\value{
The board-interlock subset of \code{edges}.
}
\description{
Convenience wrapper for \code{\link[=filter_edges_by_role]{filter_edges_by_role()}} keeping edges where both
endpoints held a board seat -- interlocking-directorate ties. \code{mode = "any"}
(the default) counts a person who \emph{ever} sat on that org's board; use
\code{mode = "primary"} to require board to be their dominant role there.
}
\examples{
\dontrun{ board_only_edges(link_cross_org(linked)$edges, profiles) }
}
