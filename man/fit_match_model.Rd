% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{fit_match_model}
\alias{fit_match_model}
\title{Fit a match model to candidate comparison vectors}
\usage{
fit_match_model(
  comparisons,
  method = c("em", "logistic"),
  labels = NULL,
  agree_cutoff = 0.5
)
}
\arguments{
\item{comparisons}{Output of \code{\link[=candidate_comparisons]{candidate_comparisons()}}.}

\item{method}{\code{"em"} or \code{"logistic"}.}

\item{labels}{Integer/logical vector of length \code{nrow(comparisons)} (\code{1}/\code{0},
\code{NA} to ignore) — required for \code{"logistic"}, ignored for \code{"em"}.}

\item{agree_cutoff}{Similarity at or above which a field counts as agreement
(EM only).}
}
\value{
An object of class \code{synthid_model}.
}
\description{
Two methods, both returning an object usable by \code{\link[=predict_match]{predict_match()}} to emit a
calibrated per-pair match probability (\code{p_match}), the confidence signal the
downstream panel model wants:
\describe{
\item{\code{"em"} (default, unsupervised)}{Fellegi-Sunter latent-class model.
Each field is binarised (similarity \verb{>= agree_cutoff} is agreement) and
\code{\link[reclin2:problink_em]{reclin2::problink_em()}} learns, by EM with \strong{no labels}, the probability
of agreement among true matches (\code{m}) and among non-matches (\code{u}) plus the
prior match rate. Weights become learned rather than hand-set; the posterior
is the naive-Bayes combination of the per-field log likelihood ratios.}
\item{\code{"logistic"} (supervised)}{Logistic regression of the label on the
graded (not binarised) similarities, so it uses the full resolution of the
comparators. Missing fields are mean-imputed with a missingness indicator.
Requires \code{labels}.}
}
}
\examples{
\dontrun{
cmp <- candidate_comparisons(panel)
em  <- fit_match_model(cmp)          # unsupervised
fs_weights(em)                       # learned agreement/disagreement weights
}
}
